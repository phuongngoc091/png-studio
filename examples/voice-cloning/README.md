# Voice Cloning Examples

These examples validate models that generate speech from a target prompt and a
reference audio file.

Use `inputs.referenceAudio` as a starter sample only. For production-quality
checks, replace it with a clean WAV reference recorded by the speaker you want to
clone. Keep the reference transcript in `inputs.referenceText` when the selected
model uses or benefits from transcript alignment.

